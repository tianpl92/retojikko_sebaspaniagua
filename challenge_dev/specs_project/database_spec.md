#Context
Generate a script SQL about next instrucctions.

#Role
Database engineer

## Database Scope

- **Database engine:** PostgreSQL
- **Database name:** `portal_plan_public_app`
- **Purpose:** Support portal user logins, store centralized public calls for proposals, and associate calls with registered users.
- **Important rule:** The issuer information for public calls is **independent** from portal user login information and must be modeled as separate data.

---

## Tables to be created

For each table, detail:
- column name
- data type
- whether it allows `NULL`
- default value, if any
- primary key designation
- any other relevant constraint

### 1) `users`

This table stores portal login user data.

**Required fields and rules:**
- `id` must be the **primary key**.
- `id`, `first_name`, `last_name`, and `email` must be **NOT NULL**.
- `gender`, `phone_number`, and `password` may be `NULL`.
- `id`, `first_name`, `last_name`, `gender`, `email`, `phone_number`, and `password` are all **alphanumeric/textual** values.
- `email` must be indexed for faster searches.

**Suggested technical specification to describe in the final instructions:**
- `id`: alphanumeric identifier type suitable for user identification, primary key.
- `first_name`: text/varchar, required.
- `last_name`: text/varchar, required.
- `gender`: text/varchar, optional.
- `email`: text/varchar, required, indexed.
- `phone_number`: text/varchar, optional.
- `password`: text/varchar, optional.

### 2) `public_calls_proposals`

This table stores centralized information about public calls for proposals.

**Required fields and rules:**
- `id` must be a **UUID primary key**.
- The `id` value must be automatically generated by default using a random UUID function.
- `call_name` must be **NOT NULL**.
- The remaining fields may be `NULL`.
- `call_name`, `call_code`, `issuer_name`, `issuer_id`, `issuer_phone`, and `issuer_email` are all **alphanumeric/textual** values.
- `call_date` must be a **timestamp**.

**Suggested technical specification to describe in the final instructions:**
- `id`: UUID, primary key, default random UUID.
- `call_name`: text/varchar, required.
- `call_code`: text/varchar, optional.
- `call_date`: timestamp, optional unless later business rules require otherwise.
- `issuer_name`: text/varchar, optional.
- `issuer_id`: text/varchar, optional.
- `issuer_phone`: text/varchar, optional.
- `issuer_email`: text/varchar, optional.

### 3) Association table between a public call and a user

This table links one public call for proposals to one registered user.

**Required fields and rules:**
- `id` must be a **UUID primary key** and automatically generated by default.
- `public_call_id` must reference the UUID primary key of the public calls table.
- `user_id` must reference the alphanumeric primary key of the `users` table.
- `association_date` must be a timestamp automatically populated with the current date/time on insertion.
- **All fields must be `NOT NULL`.**

**Suggested technical specification to describe in the final instructions:**
- `id`: UUID, primary key, default random UUID.
- `public_call_id`: UUID, required, foreign key.
- `user_id`: alphanumeric/textual, required, foreign key.
- `association_date`: timestamp, required, default current timestamp.

---

## Foreign keys to be created

Describe the foreign key relationships explicitly.

1. **Association table → `public_calls_proposals`**
   - `public_call_id` references `public_calls_proposals.id`
   - Referential action should preserve integrity (state the intended action clearly in the final SQL guidance)

2. **Association table → `users`**
   - `user_id` references `users.id`
   - Referential action should preserve integrity (state the intended action clearly in the final SQL guidance)

---

## Indexes to be created

Describe the indexes that must exist for performance.

1. **`users.email` index**
   - Required to speed up login/user lookup by email.

2. **Association table indexes**
   - Index `public_call_id` to accelerate lookups by public call.
   - Index `user_id` to accelerate lookups by user.

3. **Optional recommendation to mention if appropriate in the final implementation instructions**
   - Consider a composite index or uniqueness rule for the pair `(public_call_id, user_id)` if duplicate associations must be prevented by business logic. Only include this if it aligns with the implementation strategy.

---

## Comments that each table and field should have

Provide comment guidance for both tables and columns so the future SQL implementation is self-documenting.

### Table comments

- `users`: "Stores portal login users and their authentication/contact details."
- `public_calls_proposals`: "Stores centralized public calls for proposals and issuer metadata."
- Association table: "Links registered users to public calls for proposals."

### Field comments

#### `users`
- `id`: "Primary identifier for a portal user."
- `first_name`: "User first name."
- `last_name`: "User last name."
- `gender`: "User gender, when provided."
- `email`: "User email address used for login and contact lookup."
- `phone_number`: "User phone number, when provided."
- `password`: "User password or password reference used for authentication."

#### `public_calls_proposals`
- `id`: "Primary identifier for a public call for proposals record."
- `call_name`: "Name of the public call for proposals."
- `call_code`: "Reference code or call identifier from the source."
- `call_date`: "Timestamp indicating when the call for proposals was issued or recorded."
- `issuer_name`: "Name of the person or entity issuing the call."
- `issuer_id`: "Identification number of the person or entity issuing the call."
- `issuer_phone`: "Phone number of the issuer."
- `issuer_email`: "Email address of the issuer."

#### Association table
- `id`: "Primary identifier for the user-to-call association record."
- `public_call_id`: "Foreign key referencing the associated public call for proposals."
- `user_id`: "Foreign key referencing the registered portal user."
- `association_date`: "Timestamp when the association was created."

---

#Result final
Only generate SQL script commands sintax.
Don't run sql script.
______


